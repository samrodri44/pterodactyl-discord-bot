services:
  discord-bot:
    image: ghcr.io/samrodri44/pterodactyl-discord-bot:latest
    container_name: discord-ptero-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - /opt/pterodactyl-discord-bot/logs/:/app/logs/
